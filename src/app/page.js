import { Button } from "@/components/ui/button";
import React from "react";

const Page = () => {
  return (
    <div>
      <Button variant="secondary" size="lg">
        Hello
      </Button>
    </div>
  );
};

export default Page;
